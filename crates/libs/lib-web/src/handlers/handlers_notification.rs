use std::sync::Arc;

use axum::{
    extract::{
        ws::{Message as WsMessage, WebSocket},
        Query, State, WebSocketUpgrade,
    },
    response::IntoResponse,
};
use futures::StreamExt as _;
use serde::Deserialize;
use tracing::debug;
use uuid::Uuid;

use super::AppState;

#[derive(Deserialize)]
pub struct NotificationQuery {
    user_id: Uuid,
}

pub async fn notification_ws_handler(
    ws: WebSocketUpgrade,
    Query(params): Query<NotificationQuery>,
    State(state): State<Arc<AppState>>,
) -> impl IntoResponse {
    ws.on_upgrade(move |socket| handle_socket(socket, params.user_id, state))
}

async fn handle_socket(socket: WebSocket, user_id: Uuid, state: Arc<AppState>) {
    let (sender, mut receiver) = socket.split();

    let _ = state.notification_conns.lock().await.insert(user_id, sender);
    debug!("User {} connected to notification", user_id);

    while let Some(Ok(msg)) = receiver.next().await {
        match msg {
            WsMessage::Close(_) => break,
            _ => {}
        }
    }

    let _ = state.notification_conns.lock().await.remove(&user_id);
}

/*
me: так, как я понял оба юзера, при загрузки страницы сразу же подключается к уведомлениям, далее находит один находит другого с помощью гет запроса, далее он открывает чат с ним, далее отправляет пост запрос(или запрос на вебсокет уведомлений с ивентом new_message?) на создание сообщения(так как чата нет, он еще создает чат этим же запросом), после этого второму юзеру приходит уведомление от вебсокета, что ему поступило новое сообщение, нажимает на него и открывает чат с этим юзером и подключается к вебсокету? поправляй меня если что не так

gpt: Ты **очень правильно понял общую логику**, и у тебя уже почти всё по полочкам. Я немного **уточню и структурирую** шаги, чтобы тебе было вообще кристально ясно.

---

## ✅ Правильная последовательность:

---

### 🔌 1. **Оба юзера при загрузке страницы сразу подключаются к**:

```
/ws/notifications
```

Это **единый WebSocket** для всех событий: новых сообщений, чатов, приглашений и т.д.

```rust
Router::new()
    .route("/ws/notifications", get(notification_ws_handler))
```

---

### 👀 2. Юзер **А** ищет юзера **Б** — через `GET /users?query=...`

**Обычный HTTP-запрос.** Здесь ничего не связано с WebSocket'ами.

---

### 💬 3. Юзер А открывает с ним диалог (чат)

✅ Если чата нет — вызывается:

```rust
POST /messages
{
  "to_user_id": "...",
  "content": "Привет!"
}
```

А на бэке внутри:

```rust
send_message_to_user(...) // он создаст чат, если его нет
```

---

### 📡 4. Сервер отправляет уведомление юзеру Б через `/ws/notifications`:

```rust
let _ = tx.send(json!({
    "event": "new_message",
    "chat_id": "...",
    "from_user": "...",
    "content": "Привет!"
}).to_string());
```

> ✅ Если юзер Б онлайн — он получает это уведомление **моментально**.

---

### 💻 5. Юзер Б получает уведомление на фронте

→ кликает на него
→ клиент делает:

```http
GET /chats/:chat_id
GET /messages?chat_id=...
```

→ и затем подключается к:

```
/ws/chat/:chat_id
```

Если ты решил использовать отдельные WS для каждого чата.

---

## 🧱 Итого (кратко по шагам)

| Шаг | Кто делает | Тип запроса                   | Что происходит                                        |
| --- | ---------- | ----------------------------- | ----------------------------------------------------- |
| 1   | Оба        | WebSocket `/ws/notifications` | Подключаются                                          |
| 2   | А          | `GET /users`                  | Ищет Б                                                |
| 3   | А          | `POST /messages`              | Отправляет сообщение, при необходимости создаётся чат |
| 4   | Сервер     | `broadcast::Sender.send(...)` | Уведомляет Б через WS                                 |
| 5   | Б          | На фронте                     | Получает уведомление, подключается к чату             |

---

## ❗ Разграничение:

* **WebSocket `/ws/notifications`** — глобальный, **всегда открыт**, отвечает за все события.
* **WebSocket `/ws/chat/:chat_id`** — опционален, только если ты хочешь, чтобы чат обновлялся без перезагрузки.

---

Хочешь, могу накидать минимальный клиент (JS/WebSocket) + сервер-хендлер, чтобы прям увидеть, как это работает.


me: под каждый чат создавать отдельный вебсокет? или в одном все чаты хранить?

gpt: Отличный вопрос. И у того, и у другого подхода есть плюсы и минусы. Давай сравним прямо и чётко — а в конце я скажу, **что лучше использовать именно тебе**.

---

## ✅ Вариант 1: **Один WebSocket на все чаты**

### Как работает:

* Один `WebSocket /ws/chat` открывается при входе.
* На клиенте отправляются сообщения типа:

  ```json
  { "event": "send_message", "chat_id": "...", "content": "..." }
  ```
* На сервере ты маршрутизируешь входящие сообщения по `chat_id` и отправляешь подписчикам этого чата.

---

### Плюсы:

* ✅ Один сокет — проще менеджить на фронте и на сервере.
* ✅ Хорошо масштабируется (одно соединение — много чатов).
* ✅ Легко добавить "наблюдение" за несколькими чатами.

### Минусы:

* ❗ Больше логики на сервере — нужно чётко маршрутизировать сообщения по чатам.
* ❗ Придётся держать `HashMap<chat_id, Vec<user_id>>` или `broadcast::Sender` на каждый чат в памяти.

---

## ✅ Вариант 2: **Один WebSocket на каждый чат**

### Как работает:

* `/ws/chat/{chat_id}` — подключение отдельное под каждый чат.
* Фронт подключается к сокету только открытого чата.

---

### Плюсы:

* ✅ Просто — один сокет = один чат.
* ✅ Нет необходимости маршрутизировать по `chat_id` внутри сообщений.
* ✅ Упрощённая логика в обработке сообщений.

### Минусы:

* ❗ Нужно создавать **новое соединение на каждый чат**.
* ❗ Если юзер откроет 5 чатов — будет 5 соединений.
* ❗ В мобайл/SPA это может перегрузить соединения при плохом интернете.

---

## 💡 Что выбрать?

### 🔸 Если у тебя простой чат (в стиле Telegram или Discord, 1-2 открытых чата за раз):

➡ **WebSocket на каждый чат (`/ws/chat/{chat_id}`)** — отлично подойдёт, проще и чище логика.

### 🔸 Если ты хочешь открыть сразу **много чатов**, или использовать чат в real-time с потоками, уведомлениями, звонками:

➡ Лучше **один общий WebSocket**, и через него маршрутизируй всё по `chat_id`.

---

## 🔨 Мой совет тебе:

**Начни с одного WebSocket на каждый чат (`/ws/chat/{chat_id}`)** — тебе будет проще дебажить и понимать происходящее. Когда отшлифуешь логику — можно будет объединить в один.

Хочешь, могу показать как реализовать оба варианта, чтобы сравнить код?





*/
